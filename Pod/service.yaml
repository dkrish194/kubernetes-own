apiVersion: v1
kind: Service
metadata:
  name: python-app
  labels:
    project: ownshop
    component: frontend
spec:
  type: LoadBalancer
  selector:
    project: ownshop
    component: frontend
  ports:
    - port: 5000
      targetPort: 5000
      