apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-app-tolaratoin
  labels:
    project: roboshop
    component: frontend
  namespace: roboshop
  annotations:
    kubernetes.io/change-cause: "gree-rel-3"
spec:
  replicas: 3
  selector:
    matchLabels:
      project: roboshop
      component: frontend
  template:
    metadata:
      labels:
        project: roboshop
        component: frontend
    spec:
      containers:
        - name: python-app
          image: dkrish194/python-app:1.0.0
          env:
            - name: COLOR
              value: "green"
            - name: RELEASE
              value: "3"
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "64Mi"
              cpu: "250m"

---
apiVersion: v1
kind: Service
metadata:
  name: python-app
  labels:
    project: roboshop
    component: frontend
  namespace: roboshop
spec:
  type: LoadBalancer
  selector:
      project: roboshop
      component: frontend
  ports:
    - port: 5000
      protocol: TCP
      targetPort: 5000




